    --// locals
    local vu = game:GetService("VirtualUser")
    local plrs = game:GetService("Players")
    local plr = plrs.LocalPlayer
    local Humanoid = game.Players.LocalPlayer.Character.Humanoid
    local Mouse = plr:GetMouse()
    local UserInputService = game:GetService("UserInputService")
    local TextChatService = game:GetService("TextChatService")   
    local oldPos

    --// Functions
    local function TEL(cframe)
        if plr.Character and plr.Character:FindFirstChild("HumanoidRootPart") then
            plr.Character.HumanoidRootPart.CFrame = cframe
        end
    end

    local function getHandcuff()
            return plr.Backpack:FindFirstChild("Handcuff") or plr.Character:FindFirstChild("Handcuff")
        end

        local function To(pos)
            plr.Character.HumanoidRootPart.CFrame = pos
        end


    local function randomSeat()
        local character = plr.Character or plr.CharacterAdded:Wait()
        local humanoidRoot = character:WaitForChild("HumanoidRootPart")
        local swingSet = workspace:FindFirstChild("Swing Set")
        local validSeats = {}
        for _, swing in ipairs(swingSet:GetChildren()) do
            if swing:IsA("Model") or swing:IsA("Folder") then
                for _, part in ipairs(swing:GetDescendants()) do
                    if part:IsA("BasePart") and part.Name:lower():find("swingseat") then
                        table.insert(validSeats, part)
                    end
                end
            end
        end
        if #validSeats > 0 then
            local randomSeat = validSeats[math.random(1, #validSeats)]
            humanoidRoot.CFrame = randomSeat.CFrame
        end
    end

    local function StealGunLol()
        for _, v in pairs(plrs:GetPlayers()) do
            if v ~= plr then
                local tool = (v.Character and v.Character:FindFirstChild("Supremegun"))or
                            (v.Backpack and v.Backpack:FindFirstChild("Supremegun"))
                if tool then
                    tool.Parent = plr.Backpack
                    return tool
                end
            end
        end
        return nil
    end

    local function CrashAll()
        while _G.CrashAll do
            local gun = plr.Backpack:FindFirstChild("Supremegun")
            if not gun then
                gun = StealGunLol()
            end
            if gun and gun:FindFirstChild("RemoteEvent") then
                for _, v in pairs(plrs:GetPlayers()) do
                    if v ~= plr then
                        pcall(function()
                            gun.RemoteEvent:FireServer(v.Character)
                        end)
                    end
                end
            end
            task.wait(0.1)
        end
    end

    local function FlingPlayer(TargetPlayer)
        local Players = game:GetService("Players")
        local LocalPlayer = Players.LocalPlayer
        local Character = LocalPlayer.Character
        local Humanoid = Character and Character:FindFirstChildOfClass("Humanoid")
        local RootPart = Humanoid and Humanoid.RootPart
        local TCharacter = TargetPlayer.Character
        local THumanoid = TCharacter and TCharacter:FindFirstChildOfClass("Humanoid")
        local TRootPart = THumanoid and THumanoid.RootPart
        local THead = TCharacter and TCharacter:FindFirstChild("Head")
        local Accessory = TCharacter and TCharacter:FindFirstChildOfClass("Accessory")
        local Handle = Accessory and Accessory:FindFirstChild("Handle")
        local oldpos = RootPart.CFrame

        local FPos = function(BasePart, Pos, Ang)
            RootPart.CFrame = BasePart.CFrame * Pos * Ang
            Character:SetPrimaryPartCFrame(BasePart.CFrame * Pos * Ang)
            RootPart.Velocity = Vector3.new(9e7, 9e7 * 10, 9e7)
            RootPart.RotVelocity = Vector3.new(9e8, 9e8, 9e8)
        end
        local SFBasePart = function(BasePart)
            local start = tick()
            local Angle = 3
            while tick() - start < 2.2 do
                if not (RootPart and THumanoid and BasePart) then break end
                if BasePart.Velocity.Magnitude < 50 then
                    Angle = 30
                    FPos(BasePart, CFrame.new(0, 1.5, 0), CFrame.Angles(math.rad(Angle), 0, 0)) task.wait()
                    FPos(BasePart, CFrame.new(0, -1.5, 0), CFrame.Angles(math.rad(Angle), 0, 0)) task.wait()
                end
            end
        end
        local BV = Instance.new("BodyVelocity", RootPart)
        BV.Name = "EpixVel"
        BV.Velocity = Vector3.new(9e8, 9e8, 9e8)
        BV.MaxForce = Vector3.new(math.huge, math.huge, math.huge)

        if TRootPart and THead then
            SFBasePart((TRootPart.Position - THead.Position).Magnitude > 5 and THead or TRootPart)
        elseif TRootPart then
            SFBasePart(TRootPart)
        elseif THead then
            SFBasePart(THead)
        elseif Handle then
            SFBasePart(Handle)
        end
        BV:Destroy()
        workspace.CurrentCamera.CameraSubject = Humanoid
        RootPart.CFrame = oldpos * CFrame.new(0, 0.5, 0)
        Character:SetPrimaryPartCFrame(oldpos * CFrame.new(0, 0.5, 0))
        Humanoid:ChangeState("GettingUp")
        for _, x in ipairs(Character:GetChildren()) do
            if x:IsA("BasePart") then
                x.Velocity = Vector3.zero
                x.RotVelocity = Vector3.zero
            end
        end
    end

    local function click(cd)
        if cd and cd:IsA("ClickDetector") then fireclickdetector(cd)
        end
    end

    local function Player(TargetName)
        if TargetName == "" then
            return nil
        elseif TargetName == "random" then
            local players = game:GetService("Players"):GetPlayers()
            return players[math.random(1, #players)]
        else
            for _, v in pairs(game:GetService("Players"):GetPlayers()) do
                if v.Name:lower():sub(1, #TargetName) == TargetName:lower() or v.DisplayName:lower():sub(1, #TargetName) == TargetName then
                    return v
                end
            end
        end
    end

    --// Load Script
    local MacLib = loadstring(game:HttpGet("https://github.com/biggaboy212/Maclib/releases/latest/download/maclib.txt"))()

    local Window = MacLib:Window({
        Title = "AzzamScriptoni V2",
        Subtitle = "قيم سكني | V3.12",
        Size = UDim2.fromOffset(500, 400),
        DragStyle = 1,
        DisabledWindowControls = {},
        ShowUserInfo = true,
        Keybind = Enum.KeyCode.L,
        AcrylicBlur = true,
    })

    Window:Notify({
        Title = "LemonHub 🍋",
        Description = "منور عزام سكربتونو |  " .. plr.DisplayName,
        Lifetime = 5
    })

    Window:GlobalSetting({
        Name = "تقليل الجودة",
        Default = false,
        Callback = function(bool)
            game.ReplicatedStorage.Remotes.UpdateSetting:FireServer("LowGraphics", bool)
        end,
    })

    Window:GlobalSetting({
        Name = "ليل",
        Default = false,
        Callback = function(bool)
            game.ReplicatedStorage.Remotes.UpdateSetting:FireServer("NightMode", bool)
        end,
    })

    Window:GlobalSetting({
        Name = "مانع افك",
        Default = false,
        Callback = function(bool)
            if bool == true then
                game:GetService("Players").LocalPlayer.Idled:Connect(function()
                    vu:CaptureController()
                    vu:ClickButton2(Vector2.new())
                end)
            end
        end,
    })

    Window:GlobalSetting({
        Name = "مانع الاغتصاب",
        Default = false,
        Callback = function(bool)
            getgenv().AntiBang = bool
            if bool then
                workspace.FallenPartsDestroyHeight = 0/0
                    while getgenv().AntiBang do
                        wait(0.1)
                        for _, v in pairs(plrs:GetPlayers()) do
                            if v ~= plr and v.Character and v.Character:FindFirstChild("HumanoidRootPart") and
                            plr.Character and plr.Character:FindFirstChild("HumanoidRootPart") then
                                if (plr.Character.HumanoidRootPart.Position - v.Character.HumanoidRootPart.Position).Magnitude <= 2 then
                                    local pos = plr.Character.HumanoidRootPart.CFrame
                                    TEL(CFrame.new(10, -1000, 10))
                                    wait()
                                    TEL(pos)
                                end
                            end
                        end
                    end
            else
                getgenv().AntiBang = false
            end
        end,
    })
   Window:GlobalSetting({
        Name = "مانع فلينق",
        Default = false,
        Callback = function(bool)
         if bool == true then
			AntiFlingLoop = game:GetService('RunService').Stepped:Connect(function()
				for _,p in pairs(game.Players:GetPlayers()) do
					task.spawn(function()
						if p ~= game.Players.LocalPlayer and p.Character then
							for _,v in pairs(p.Character:GetChildren()) do
								pcall(function()
									if v:IsA("BasePart") then
										v.CanCollide = false
										v.Velocity = Vector3.new(0,0,0)
										v.RotVelocity = Vector3.new(0,0,0)
										v.CustomPhysicalProperties = PhysicalProperties.new(0,0,0,0,0)
									end
								end)
							end
						end
					end)
				end
			end)
		else
			AntiFlingLoop:Disconnect()
			for _,p in pairs(game.Players:GetPlayers()) do
				task.spawn(function()
					if p ~= game.Players.LocalPlayer and p.Character then
						for _,v in pairs(p.Character:GetChildren()) do
							pcall(function()
								if v:IsA("BasePart") then
									v.CanCollide = true
								end
							end)
						end
					end
				end)
			end
		end
	end,
    })
    Window:GlobalSetting({
        Name = "اعادة دخول للسيرفر",
        Callback = function()
            game:GetService("TeleportService"):Teleport(game.PlaceId)
        end,
    })

    local TG = Window:TabGroup()

    local Game = TG:Tab({
        Name = "Game",
        Image = "rbxassetid://11419714821"
    })

    local Section = Game:Section({
        Side = "Left"
    })

local WhiteList = {
    "karer78l0",
    "rektifn",
    "TypicalAndy",
    "1haidan",
    "7zen_lemon",
    "ALIGMSKG1",
    "SXLBinim",
}

local function isWhitelisted(name)
    for _, v in pairs(WhiteList) do
        if v == name then
            return true
        end
    end
    return false
end

local Ilovethis = {
    "OwO", "UwU", ">w<", "^w^", ":3", ":>", ":)", ":D",
    "x3", "^-^", "^.^", "owo", "uvu", ":)"
}

local function Cute()
    return Ilovethis[math.random(1, #Ilovethis)]
end

Section:Button({
    Name = "كلبشة مجانية",
    Callback = function()
        for _, p in pairs(game.Players:GetPlayers()) do
            if p ~= plr and p:FindFirstChild("Backpack") then
                local h = p.Backpack:FindFirstChild("Handcuff")
                if h and h:IsA("Tool") then
                    h.Parent = plr.Backpack
                    return
                end
            end
        end
        Window:Notify({
            Title = "LemonHub",
            Description = "مافي كلبشات بالسيرفر",
            Lifetime = 5
        })
    end,
})

pcall(function()
    if isWhitelisted(plr.Name) then
        Window:Notify({
            Title = "LemonHub 🍋",
            Description = "وايت ليست | " .. Cute(),
            Lifetime = 5
        })

        Section:Button({
            Name = "تكريش الكل",
            Callback = function()
                CrashAll()
            end
        })
    end
end)


    local RemovedRatings = {}

    Section:Toggle({
        Name = "اخفاء التقييم",
        Default = false,
        Callback = function(bool)
            getgenv().HideRa = bool
            if bool then
                RemovedRatings = {}
                    while getgenv().HideRa do
                        for _, p in pairs(game.Players:GetPlayers()) do
                            local c = p.Character
                            if c then
                                local r = c:FindFirstChild("ratingProx", true)
                                if r and not RemovedRatings[r] then
                                    RemovedRatings[r] = r.Parent
                                    r.Parent = nil --// its me lol
                                end
                            end
                        end
                        task.wait(0.5)
                    end
            else
                for rating, parent in pairs(RemovedRatings) do
                    if rating and parent then
                        rating.Parent = parent
                    end
                end
                RemovedRatings = {}
            end
        end,
    })


    Section:Toggle({
         Name = "مضاد السجن",
         Default = false,
        Callback = function(bool)
             getgenv().AntiJail = bool
            if bool then
                    while getgenv().AntiJail do
                        for _, v in pairs(workspace:GetChildren()) do
                            if v.Name:lower():find("jail") then
                                v:Destroy()
                            end
                        end
                        task.wait(.1)
                    end
            end
        end,
    })

    local Sc

    Section:Toggle({
        Name = "مضاد الجلوس",
        Default = false,
        Callback = function(bool)
            if bool then
                Sc = (plr.Character or plr.CharacterAdded:Wait()):WaitForChild("Humanoid").Seated:Connect(function(active)
                    if active then plr.Character.Humanoid:ChangeState(Enum.HumanoidStateType.Running) end
                end)
            elseif Sc then
                Sc:Disconnect()
                Sc = nil
            end
        end
    })

    Section:Toggle({
        Name = "سبام بطاطس",
        Default = false,
        Callback = function(bool)
            getgenv().SpamPotato = bool
                local keys = workspace["Vending Machine"].Keypad.Buttons.Keys
                local enter = workspace["Vending Machine"].Keypad.Buttons.Enter.ClickDetector
                local codes = {
                            {"1", "B"},
                            {"1", "C"},
                            {"2", "A"},
                            {"2", "B"},
                            {"2", "C"},
                            {"3", "A"},
                            {"3", "B"},
                            {"3", "C"},
                        }
                while getgenv().SpamPotato and task.wait(0.1) do
                    for _, c in ipairs(codes) do
                        click(keys[c[1]].ClickDetector)
                        task.wait()
                        click(keys[c[2]].ClickDetector)
                        task.wait()
                        click(enter)
                        task.wait()
                    end
                end
        end,
    })
Section:Toggle({
    Name = "فلينق الكل",
    Default = false,
    Callback = function(bool)
        getgenv().FlingAll = bool
        if bool then
            task.spawn(function()
                while getgenv().FlingAll do
                    for _, v in pairs(plrs:GetPlayers()) do
                        if not getgenv().FlingAll then break end
                        if v ~= plr then
                            FlingPlayer(v)
                            randomSeat()
                            task.wait(0.2)
                        end
                    end
                    task.wait(0.5)
                end
            end)
        end
    end
})

    Section:SubLabel({
        Text = "معوق"
    })


  Section:Toggle({
    Name = "سبام طبول",
    Default = false,
    Callback = function(bool)
        getgenv().DrumKit = bool
        if bool then
                while getgenv().DrumKit do
                    for _, v in ipairs(workspace.DrumKit:GetDescendants()) do
                        if v:IsA("ClickDetector") then
                            click(v)
                        end
                    end
                    task.wait(0.1)
                end
        end
    end,
})


    local Section2 = Game:Section({
        Side = "Right"
    })

    Section2:Input({
        Name = "السرعة",
        Placeholder = "رقم",
        AcceptedCharacters = function(input)
            return input:gsub("[^0-9]", "")
        end,
        Callback = function(input)
            local humanoid = game.Players.LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
                if input == "" then
                    humanoid.WalkSpeed = 16
                else
                    humanoid.WalkSpeed = tonumber(input)
                end
        end,
    }, "SpeedInput")

    Section2:Input({
        Name = "القفز",
        Placeholder = "رقم",
        AcceptedCharacters = function(input)
            return input:gsub("[^0-9]", "") 
        end,
        Callback = function(input)
            local humanoid = game.Players.LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
                if input == "" then
                    humanoid.JumpPower = 50
                else
                    humanoid.JumpPower = tonumber(input)
                end
        end,
    }, "JumpInput")

 Section2:Button({
    Name = "ريست",
    Callback = function()
        local pos = game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame
        game.Players.LocalPlayer.Character.Humanoid.Health = 0
        local char = game.Players.LocalPlayer.CharacterAdded:Wait()
        local hrp = char:WaitForChild("HumanoidRootPart")
        for i = 1, 5 do
            hrp.CFrame = pos
            task.wait(0.1)
        end
    end,
})

  Section2:Toggle({
    Name = "اختراق الجدار",
    Default = false,
    Callback = function(bool)
       if bool == true then
            game:GetService("RunService"):BindToRenderStep("Noclip", 0, function()
                for i, v in pairs(game.Players.LocalPlayer.Character:GetChildren()) do
                    if v:IsA("BasePart") and v.CanCollide then
                        v.CanCollide = false
                    end
                end
            end)
        else
            game:GetService("RunService"):UnbindFromRenderStep("Noclip")
            for i, v in pairs(game.Players.LocalPlayer.Character:GetChildren()) do
                if v:IsA("BasePart") and not v.CanCollide then
                    v.CanCollide = true
                end
            end
        end
    end,
})
    local TG2 = Window:TabGroup()

    local Target = TG2:Tab({
        Name = "Target",
        Image = "rbxassetid://127591567076871"
    })

    local Section = Target:Section({
        Side = "Left"
    })

    getgenv().SelectTarget = nil

    Section:Input({
        Name = "استهداف",
        Placeholder = "اسم اللاعب",
        AcceptedCharacters = function(input)
            return input
        end,
        Callback = function(Name)
            getgenv().SelectTarget = Name
            local Target = Player(Name)
            if Target and Target ~= plr then
                local hasCuff = Target:FindFirstChild("Backpack") and Target.Backpack:FindFirstChild("Handcuff")
                game:GetService("StarterGui"):SetCore("SendNotification", {
                    Title = "اللاعب : " .. Target.DisplayName,
                    Text = "",
                    Icon = "rbxthumb://type=AvatarHeadShot&id=" .. Target.UserId .. "&w=420&h=420",
                    Duration = 5
                })
            end
        end,
    }, "TargetInput")

   local function performKill(restorePosition)
            local Character = plr.Character or plr.CharacterAdded:Wait()
            local Backpack = plr:WaitForChild("Backpack")
            local Target = Player(getgenv().SelectTarget)

            local handcuff = getHandcuff()
            if not handcuff then
                Window:Notify({
                    Title = "LemonHub",
                    Description = "ماعندك كلبشة",
                    Lifetime = 5
                })
                return
            end

            workspace.FallenPartsDestroyHeight = 0/0

            if handcuff.Parent == Backpack then
                handcuff.Parent = Character
                task.wait(0.5)
            end

            local oldPos = Character.HumanoidRootPart.CFrame
            To(Target.Character.HumanoidRootPart.CFrame * CFrame.new(0, 0, -5))
            task.wait(0.5)

            handcuff:WaitForChild("ChangeTarget"):FireServer(Target.Character)
            task.wait(0.5)

            To(CFrame.new(254.883865, -481.286896, 233.496552, 0.864870429, 0.491252154, -0.10329783, -9.7598889e-09, 0.205774575, 0.978599429, 0.501995146, -0.846361697, 0.177968353))
            task.wait(1)

            handcuff:WaitForChild("ChangeTarget"):FireServer(nil)
            task.wait(1)

            if restorePosition then
                To(oldPos)
            else
                local head = Character:FindFirstChild("Head")
                if head then head:Destroy() end
            end
        end

    Section:Button({
        Name = "توول اختيار اللاعب",
        Callback = function()
            local a = plr.Backpack:FindFirstChild("ClickTarget") or plr.Character:FindFirstChild("ClickTarget")
            if a then a:Destroy() end
            local tool = Instance.new("Tool")
            tool.Name = "ClickTarget"
            tool.RequiresHandle = false
            tool.TextureId = "rbxassetid://126606886340521"
            tool.Parent = plr.Backpack
            tool.Activated:Connect(function()
                local target = Mouse.Target
                local model = target:FindFirstAncestorOfClass("Model")
                if model and game.Players:GetPlayerFromCharacter(model) then
                    local player = game.Players:GetPlayerFromCharacter(model)
                    getgenv().SelectTarget = player.Name
                    game:GetService("StarterGui"):SetCore("SendNotification", {
                        Title = "اللاعب : " .. player.DisplayName,
                        Text = "",
                        Icon = "rbxthumb://type=AvatarHeadShot&id=" .. player.UserId .. "&w=420&h=420",
                        Duration = 5
                    })
                end
            end)
        end,
    })


    Section:Toggle({
        Name = "فلينق",
        Default = false,
        Callback = function(bool)
            getgenv().Fling = bool
                while getgenv().Fling do
                    local Target = Player(getgenv().SelectTarget)
                        randomSeat()
                        task.wait(0.1)
                        FlingPlayer(Target)
                    end
         task.wait(.1)
        end,
    })

    Section:Button({
        Name = "سحبه فوق الماب",
        Callback = function()
           local Target = Player(getgenv().SelectTarget)
            local HRP = plr.Character and plr.Character:FindFirstChild("HumanoidRootPart")
            if not plr.Backpack:FindFirstChild("Handcuff") then
                Window:Notify({
                    Title = "LemonHub",
                    Description = "ماعندك كلبشة",
                    Lifetime = 5
                })
                return
            end
            HRP.CFrame = Target.Character.HumanoidRootPart.CFrame
            wait(0.3)
            local handcuff = plr.Backpack.Handcuff
            handcuff.ChangeTarget:FireServer(Target.Character)
            wait(0.1)
            HRP.CFrame = CFrame.new(519, 164, 149)
            task.wait(0.3)
            handcuff:WaitForChild("ChangeTarget"):FireServer(nil)
        end,
    })

    Section:Button({
        Name = "سجنه",
        Callback = function()
            local Target = Player(getgenv().SelectTarget)
            local ROOT = game.Players.LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
            local oldpos = ROOT.CFrame
            ROOT.CFrame = Target.Character.HumanoidRootPart.CFrame
            task.wait(0.4)
            game.ReplicatedStorage.HDAdminHDClient.Signals.RequestCommandSilent:InvokeServer(";jc")
            ROOT.CFrame = oldpos
        end,
    })

    Section:Button({
        Name = "سحب",
        Callback = function()
            local Target = Player(getgenv().SelectTarget)
            local HRP = plr.Character and plr.Character:FindFirstChild("HumanoidRootPart")
            if not plr.Backpack:FindFirstChild("Handcuff") then
                Window:Notify({
                    Title = "LemonHub",
                    Description = "ماعندك كلبشة",
                    Lifetime = 5
                })
                return
            end
            local Oldpos = HRP.CFrame
            HRP.CFrame = Target.Character.HumanoidRootPart.CFrame
            wait(0.3)
            local handcuff = plr.Backpack.Handcuff
            handcuff.ChangeTarget:FireServer(Target.Character)
            wait(0.1)
            HRP.CFrame = Oldpos
        end,
    })

        Section:Button({
            Name = "V1 قتل",
            Callback = function()
                performKill(false)
            end,
        })

        Section:Button({
            Name = "V2 قتل",
            Callback = function()
                performKill(true)
            end,
        })

    Section:Button({
        Name = "انتقال",
        Callback = function()
        local Target = Player(getgenv().SelectTarget)
        game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = Target.Character.HumanoidRootPart.CFrame * CFrame.new(2,0,0)
        end,
    })

    Section:Toggle({
        Name = "مشاهدة",
        Default = false,
        Callback = function(bool)
            local Target = Player(getgenv().SelectTarget)
            if bool then game.Workspace.Camera.CameraSubject = Target.Character.Humanoid else game.Workspace.Camera.CameraSubject = game.Players.LocalPlayer.Character.Humanoid
        end
    end,
    })
    Section:Button({
        Name = "سرقة الانميشن",
        Callback = function()
            local t = Player(getgenv().SelectTarget)
            if not t or t == plr or not t.Character or t.Character:FindFirstChild("Humanoid") and t.Character.Humanoid.RigType == Enum.HumanoidRigType.R6 then return end
            local a1, a2 = plr.Character:FindFirstChild("Animate"), t.Character:FindFirstChild("Animate")
            if a1 and a2 then
                a1:ClearAllChildren()
                for _, v in ipairs(a2:GetChildren()) do
                    v:Clone().Parent = a1
                end
            end
        end
    })

      local sex = Target:Section({
        Side = "Right"
    })

    --// Sex
    function CustomeTeleport(POS)
        local Target = Player(getgenv().SelectTarget)
        game.Players.LocalPlayer.Character.HumanoidRootPart.Velocity = Vector3.new(0,0,0)
        game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = Target.Character.HumanoidRootPart.CFrame * POS
    end

    function Suck(POS)
        local Target = Player(getgenv().SelectTarget)
        if Target and Target.Character and Target.Character:FindFirstChild("HumanoidRootPart") then
            local TCH = Target.Character.HumanoidRootPart
            local R = game.Players.LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
            if TCH and R then
                R.CFrame = TCH.CFrame * CFrame.new(0, -2, 0) * CFrame.Angles(0, math.pi, 0) * POS
            end
        end
    end

    local function FaceBang(POS)
        local Target = Player(getgenv().SelectTarget)
        local TH = Target.Character and Target.Character:FindFirstChild("Head")
        local myHRP = game.Players.LocalPlayer.Character and game.Players.LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
        if TH and myHRP then
            myHRP.Velocity = Vector3.new(0, 0, 0)
            local faceDirection = TH.CFrame.LookVector
            local newCFrame = TH.CFrame + (faceDirection * POS.Z) + Vector3.new(0, 1, 0)
            myHRP.CFrame = newCFrame * CFrame.Angles(0, math.rad(180), 0)
        end
    end

     sex:Toggle({
        Name = "Bang",
        Default = false,
	    Callback = function(bool)
            if bool then
            getgenv().Bang = true
            Humanoid.PlatformStand = true
            repeat
                task.wait()
                game.Players.LocalPlayer.Character.Humanoid.Sit = false
                CustomeTeleport(CFrame.new(0, 0, 1))
                task.wait()
                game.Players.LocalPlayer.Character.Humanoid.Sit = false
                CustomeTeleport(CFrame.new(0, 0, 1.5))
                task.wait()
                game.Players.LocalPlayer.Character.Humanoid.Sit = false
                CustomeTeleport(CFrame.new(0, 0, 2))
                task.wait()
                game.Players.LocalPlayer.Character.Humanoid.Sit = false
                CustomeTeleport(CFrame.new(0, 0, 2.5))
                task.wait()
                game.Players.LocalPlayer.Character.Humanoid.Sit = false
                CustomeTeleport(CFrame.new(0, 0, 3))
                task.wait()
                game.Players.LocalPlayer.Character.Humanoid.Sit = false
                CustomeTeleport(CFrame.new(0, 0, 3.5))
                task.wait()
                game.Players.LocalPlayer.Character.Humanoid.Sit = false
                CustomeTeleport(CFrame.new(0, 0, 4))
                task.wait()
                game.Players.LocalPlayer.Character.Humanoid.Sit = false
                CustomeTeleport(CFrame.new(0, 0, 3.5))
                task.wait()
                game.Players.LocalPlayer.Character.Humanoid.Sit = false
                CustomeTeleport(CFrame.new(0, 0, 3))
                task.wait()
                game.Players.LocalPlayer.Character.Humanoid.Sit = false
                CustomeTeleport(CFrame.new(0, 0, 2.5))
                task.wait()
                game.Players.LocalPlayer.Character.Humanoid.Sit = false
                CustomeTeleport(CFrame.new(0, 0, 2))
                task.wait()
                game.Players.LocalPlayer.Character.Humanoid.Sit = false
                CustomeTeleport(CFrame.new(0, 0, 1.5))
                task.wait()
                game.Players.LocalPlayer.Character.Humanoid.Sit = false
                CustomeTeleport(CFrame.new(0, 0, 1))
            until getgenv().Bang == false
        else
            getgenv().Bang = false
            Humanoid.PlatformStand = false
        end
    end,
    })
    sex:Toggle({
        Name = "FaceBang",
        Default = false,
	    Callback = function(bool)
            if bool then
            getgenv().FaceBang = true
            Humanoid.PlatformStand = true
            repeat
                task.wait()
                game.Players.LocalPlayer.Character.Humanoid.Sit = false
                FaceBang(CFrame.new(0, 0, 1))
                task.wait()
                game.Players.LocalPlayer.Character.Humanoid.Sit = false
                FaceBang(CFrame.new(0, 0, 1.5))
                task.wait()
                game.Players.LocalPlayer.Character.Humanoid.Sit = false
                FaceBang(CFrame.new(0, 0, 2))
                task.wait()
                game.Players.LocalPlayer.Character.Humanoid.Sit = false
                FaceBang(CFrame.new(0, 0, 2.5))
                task.wait()
                game.Players.LocalPlayer.Character.Humanoid.Sit = false
                FaceBang(CFrame.new(0, 0, 3))
                task.wait()
                game.Players.LocalPlayer.Character.Humanoid.Sit = false
                FaceBang(CFrame.new(0, 0, 3.5))
                task.wait()
                game.Players.LocalPlayer.Character.Humanoid.Sit = false
                FaceBang(CFrame.new(0, 0, 4))
                task.wait()
                game.Players.LocalPlayer.Character.Humanoid.Sit = false
                FaceBang(CFrame.new(0, 0, 3.5))
                task.wait()
                game.Players.LocalPlayer.Character.Humanoid.Sit = false
                FaceBang(CFrame.new(0, 0, 3))
                task.wait()
                game.Players.LocalPlayer.Character.Humanoid.Sit = false
                FaceBang(CFrame.new(0, 0, 2.5))
                task.wait()
                game.Players.LocalPlayer.Character.Humanoid.Sit = false
                FaceBang(CFrame.new(0, 0, 2))
                task.wait()
                game.Players.LocalPlayer.Character.Humanoid.Sit = false
                FaceBang(CFrame.new(0, 0, 1.5))
                task.wait()
                game.Players.LocalPlayer.Character.Humanoid.Sit = false
                FaceBang(CFrame.new(0, 0, 1))
            until getgenv().FaceBang == false
        else
            Humanoid.PlatformStand = false
            getgenv().FaceBang = false
        end

    end,
    })

    sex:Toggle({
        Name = "Suck",
        Default = false,
	    Callback = function(bool)
             if bool then
            getgenv().Suck = true
            repeat task.wait()
            task.wait() game.Players.LocalPlayer.Character.Humanoid.Sit = true Suck(CFrame.new(0, 0, 1))
            task.wait() game.Players.LocalPlayer.Character.Humanoid.Sit = true Suck(CFrame.new(0, 0, 1.5))
            task.wait() game.Players.LocalPlayer.Character.Humanoid.Sit = true Suck(CFrame.new(0, 0, 2))
            task.wait() game.Players.LocalPlayer.Character.Humanoid.Sit = true Suck(CFrame.new(0, 0, 2.5))
            task.wait() game.Players.LocalPlayer.Character.Humanoid.Sit = true Suck(CFrame.new(0, 0, 3))
            task.wait() game.Players.LocalPlayer.Character.Humanoid.Sit = true Suck(CFrame.new(0, 0, 3.5))
            task.wait() game.Players.LocalPlayer.Character.Humanoid.Sit = true Suck(CFrame.new(0, 0, 4))
            task.wait() game.Players.LocalPlayer.Character.Humanoid.Sit = true Suck(CFrame.new(0, 0, 3.5))
            task.wait() game.Players.LocalPlayer.Character.Humanoid.Sit = true Suck(CFrame.new(0, 0, 3))
            task.wait() game.Players.LocalPlayer.Character.Humanoid.Sit = true Suck(CFrame.new(0, 0, 2.5))
            task.wait() game.Players.LocalPlayer.Character.Humanoid.Sit = true Suck(CFrame.new(0, 0, 2))
            task.wait() game.Players.LocalPlayer.Character.Humanoid.Sit = true Suck(CFrame.new(0, 0, 1.5))
            task.wait() game.Players.LocalPlayer.Character.Humanoid.Sit = true Suck(CFrame.new(0, 0, 1))
            until getgenv().Suck == false
        else
            getgenv().Suck = false
            game.Players.LocalPlayer.Character.Humanoid.Sit = false
        end
    end,
    })

    sex:Toggle({
        Name = "Benx",
        Default = false,
	    Callback = function(bool)
            if bool then
            getgenv().Benx = true
            repeat task.wait()
                    task.wait() game.Players.LocalPlayer.Character.Humanoid.Sit = true CustomeTeleport(CFrame.new(0, -0.4, -1) * CFrame.Angles(-1.5,math.rad(0),0))
                    task.wait() game.Players.LocalPlayer.Character.Humanoid.Sit = true CustomeTeleport(CFrame.new(0, -0.4, -1.5) * CFrame.Angles(-1.5,math.rad(0),0))
                    task.wait() game.Players.LocalPlayer.Character.Humanoid.Sit = true CustomeTeleport(CFrame.new(0, -0.4, -2) * CFrame.Angles(-1.5,math.rad(0),0))
                    task.wait() game.Players.LocalPlayer.Character.Humanoid.Sit = true CustomeTeleport(CFrame.new(0, -0.4, -2.5) * CFrame.Angles(-1.5,math.rad(0),0))
                    task.wait() game.Players.LocalPlayer.Character.Humanoid.Sit = true CustomeTeleport(CFrame.new(0, -0.4, -3) * CFrame.Angles(-1.5,math.rad(0),0))
                    task.wait() game.Players.LocalPlayer.Character.Humanoid.Sit = true CustomeTeleport(CFrame.new(0, -0.4, -3.5) * CFrame.Angles(-1.5,math.rad(0),0))
                    task.wait() game.Players.LocalPlayer.Character.Humanoid.Sit = true CustomeTeleport(CFrame.new(0, -0.4, -4) * CFrame.Angles(-1.5,math.rad(0),0))
                    task.wait() game.Players.LocalPlayer.Character.Humanoid.Sit = true CustomeTeleport(CFrame.new(0, -0.4, -3.5) * CFrame.Angles(-1.5,math.rad(0),0))
                    task.wait() game.Players.LocalPlayer.Character.Humanoid.Sit = true CustomeTeleport(CFrame.new(0, -0.4, -3) * CFrame.Angles(-1.5,math.rad(0),0))
                    task.wait() game.Players.LocalPlayer.Character.Humanoid.Sit = true CustomeTeleport(CFrame.new(0, -0.4, -2.5) * CFrame.Angles(-1.5,math.rad(0),0))
                    task.wait() game.Players.LocalPlayer.Character.Humanoid.Sit = true CustomeTeleport(CFrame.new(0, -0.4, -2) * CFrame.Angles(-1.5,math.rad(0),0))
                    task.wait() game.Players.LocalPlayer.Character.Humanoid.Sit = true CustomeTeleport(CFrame.new(0, -0.4, -1.5) * CFrame.Angles(-1.5,math.rad(0),0))
                    task.wait() game.Players.LocalPlayer.Character.Humanoid.Sit = true CustomeTeleport(CFrame.new(0, -0.4, -1) * CFrame.Angles(-1.5,math.rad(0),0))
            until getgenv().Benx == false
        else
            getgenv().Benx = false
            game.Players.LocalPlayer.Character.Humanoid.Sit = false
        end
    end,
    })

    sex:Toggle({
        Name = "Headsit",
        Default = false,
	    Callback = function(bool)
         local Target = Player(getgenv().SelectTarget)
        getgenv().Headsit = bool
        if bool then
            while getgenv().Headsit do
                task.wait()
                game.Players.LocalPlayer.Character.HumanoidRootPart.Velocity = Vector3.new(0, 0, 0)
                game.Players.LocalPlayer.Character.Humanoid.Sit = true
                game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = Target.Character.Head.CFrame * CFrame.new(0, 0.6, 1.5)
            end
        else
            game.Players.LocalPlayer.Character.HumanoidRootPart.Velocity = Vector3.new(0, 0, 0)
        end
    end,
 })
  sex:Toggle({
        Name = "Facesit",
        Default = false,
	    Callback = function(bool)
        local Target = Player(getgenv().SelectTarget)
        getgenv().Headsit = bool
        if bool then
            while getgenv().Headsit do
                task.wait()
                local H = game.Players.LocalPlayer.Character.HumanoidRootPart
                local head = Target.Character.Head
                H.Velocity = Vector3.zero
                H.CFrame = head.CFrame * CFrame.new(0, 0, -1.0) * CFrame.Angles(0, math.pi, 0)
                game.Players.LocalPlayer.Character.Humanoid.Sit = true
            end
        else
            game.Players.LocalPlayer.Character.HumanoidRootPart.Velocity = Vector3.new(0, 0, 0)
        end
    end,
 })

  sex:Button({
    Name = "نسخ سكنه",
    Callback = function()
        local Target = Player(getgenv().SelectTarget)
        TextChatService.TextChannels.RBXGeneral:SendAsync(";char me " .. Target.Name)
    end
})

  sex:Button({
    Name = "نسخ اسمه",
    Callback = function()
        local Target = Player(getgenv().SelectTarget)
        TextChatService.TextChannels.RBXGeneral:SendAsync(";Name me " .. Target.Name)
    end
})

 local Teleport = TG2:Tab({
    Name = "Teleport",
    Image = "rbxassetid://92576879355188"
})


local Tpos = {
    Grass     = Vector3.new(-78, 4, 101),
    Start     = Vector3.new(253, 8, 136),
    Top1      = Vector3.new(177, 164, -108),
    Top2      = Vector3.new(519, 164, 149),
    Top3      = Vector3.new(248, 164, 349),
    Board     = Vector3.new(189, 25, 62),
    XO        = Vector3.new(207, 12, -6),
    Piano     = Vector3.new(156, 10, 1),
    Tree      = Vector3.new(362, 70, 56),
    Stand     = Vector3.new(251, 46, 55),
}

local T = Teleport:Section({
    Side = "Right"
})

local Tween = Teleport:Section({
    Side = "Left"
})


Tween:Toggle({
    Name = "كليك تلبورت",
    Default = false,
    Callback = function(bool)
        getgenv().Click = bool
    end,
})

UserInputService.InputBegan:Connect(function(input, gameProcessed)
    if gameProcessed then return end
    if getgenv().Click and input.UserInputType == Enum.UserInputType.MouseButton1 then --// MouseLeft
        local hrp = plr.Character and plr.Character:FindFirstChild("HumanoidRootPart")
            hrp.CFrame = CFrame.new(Mouse.Hit.p)
        end
end)


local bn = {
    Grass = "العشب",
    Start = "البداية",
    Top1 = "فوق 1",
    Top2 = "فوق 2",
    Top3 = "فوق 3",
    Board = "فوق اللوحة",
    XO = "منطقة XO",
    Piano = "البيانو",
    Tree = "الشجرة",
    Stand = "الستاند",
}

Tween:Button({
    Name = "SaveZone",
    Callback = function()
        workspace.FallenPartsDestroyHeight = 0/0
        local cf = CFrame.new(27685.3691, -52256.3906, -7714.5835, -0.638667285, 0.0114277983, 0.769398153, -4.58e-10, 0.999889731, -0.0148512684, -0.76948303, -0.00948501937, -0.638596833)
        local p = Instance.new("Part", workspace)
        p.Size = Vector3.new(6, 1, 6)
        p.Anchored = true
        p.BrickColor = BrickColor.Gray()
        p.CFrame = cf
        local hrp = plr.Character or plr.CharacterAdded:Wait()
        hrp:WaitForChild("HumanoidRootPart").CFrame = cf + Vector3.new(0, 5, 0)
    end
})

Tween:Button({
    Name = "حفظ المكان",
    Callback = function()
        local hrp = game.Players.LocalPlayer.Character.HumanoidRootPart
        oldPos = hrp.CFrame
        local pos = hrp.Position
        Window:Notify({
            Title = "LemonHub",
            Description = ("تم حفظ المكان ! | X : %.2f Y : %.2f Z : %.2f"):format(pos.X, pos.Y, pos.Z),
            Lifetime = 3
        })
    end
})


Tween:Button({
    Name = "المكان السابق",
    Callback = function()
        if oldPos then
            game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = oldPos
            local pos = oldPos.Position
            Window:Notify({
                Title = "LemonHub",
                Description = ("تم الرجوع للمكان السابق ! | X : %.2f Y : %.2f Z : %.2f"):format(pos.X, pos.Y, pos.Z),
                Lifetime = 3
            })
        end
    end
})


for key, position in pairs(Tpos) do
    local name = bn[key] or key
    T:Button({
        Name = "انتقال " .. name,
        Callback = function()
            game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(position)
        end,
    })
end

    --[[local Animation = TG2:Tab({ كنسل يرجال زوط
        Name = "Anim",
        Image = "rbxassetid://105590275557472"
    })]]

    local commands = TG2:Tab({
        Name = "Cmd",
        Image = "rbxassetid://96018204407050"
    })


    local commands = commands:Section({
        Side = "Left"
    })

    commands:Toggle({
        Name = "سبام اسم ملون",
        Default = false,
        Callback = function(bool)
            getgenv().SPAMNAME = bool
            if bool then
                    while getgenv().SPAMNAME do
                        local nameList = {
                            ";name me ⬜⬜⬜⬜⬜⬜⬜⬜⬜⬜⬜⬜⬜⬜⬜⬜⬜⬜⬜⬜⬜⬜⬜⬜⬜⬜⬜⬜⬜⬜⬜⬜⬜⬜⬜",
                            ";name me ⬛⬛⬛⬛⬛⬛⬛⬛⬛⬛⬛⬛⬛⬛⬛⬛⬛⬛⬛⬛⬛⬛⬛⬛⬛⬛⬛⬛⬛⬛⬛⬛⬛⬛⬛",
                            ";name me 🟨🟨🟨🟨🟨🟨🟨🟨🟨🟨🟨🟨🟨🟨🟨🟨🟨🟨🟨🟨🟨🟨🟨🟨🟨🟨🟨🟨🟨🟨🟨🟨🟨🟨🟨",
                        }
                        for _, v in ipairs(nameList) do
                            if not getgenv().SPAMNAME then break end
                            game:GetService("ReplicatedStorage").HDAdminHDClient.Signals.RequestCommandSilent:InvokeServer(v)
                           wait(6)
                        end
                    end
            end
        end
    })

    commands:Toggle({
        Name = "دخان",
        Default = false,
        Callback = function(bool)
            local cmd = bool and ";smoke" or ";unsmoke"
            game:GetService("ReplicatedStorage"):WaitForChild("HDAdminHDClient") :WaitForChild("Signals"):WaitForChild("RequestCommand"):InvokeServer(cmd)
        end
    })

    commands:Divider()

local run

commands:Toggle({
    Name = "سبام Title",
    Default = false,
    Callback = function(bool)
        _G.Titleelelelell = bool
        local titles = {";title", ";titley", ";titlep", ";titleo", ";titlepk", ";titlebk"}
        if bool then
            run = task.spawn(function()
                while _G.Titleelelelell do
                    for _, cmd in ipairs(titles) do
                        if not _G.Titleelelelell then break end
                        game:GetService("ReplicatedStorage").HDAdminHDClient.Signals.RequestCommandSilent:InvokeServer(cmd.." me LemonHub On Top")
                        task.wait(2)
                    end
                end
            end)
        else
            _G.Titleelelelell = false
            game:GetService("ReplicatedStorage").HDAdminHDClient.Signals.RequestCommandSilent:InvokeServer(";title me ")
        end
    end
})


commands:Button({
    Name = "سكن منور",
    Callback = function()
        for i, id in ipairs({
            74891470, 132809431, 215718515, 128217885, 183468963,
            92719239, 417456127, 16733494880, 150381051, 192557913,
            173624561, 173624602, 439944088, 173624561, 173624602,
            9560383533, 89171071, 173624651
        }) do
            game:GetService("ReplicatedStorage").HDAdminHDClient.Signals.RequestCommandSilent:InvokeServer(";hat me "..id)
            task.wait(
                i == 1 and 0.7 or
                i == 2 and 0.8 or
                i == 3 and 0.9 or
                i == 4 and 1 or
                i == 5 and 0.9 or
                i == 6 and 0.8 or
                i == 7 and 1 or
                i == 8 and 0.8 or
                i == 9 and 0.9 or
                i == 10 and 1 or
                i == 11 and 0.8 or
                i == 12 and 1 or
                i == 13 and 0.9 or
                i == 14 and 0.8 or
                i == 15 and 1 or
                i == 16 and 1 or
                i == 17 and 0.9 or
                i == 18 and 0.8 or
                1
            )
        end
    end
})

commands:Divider()

commands:Button({
    Name = "سكن الاونر",
    Callback = function()
        game:GetService("TextChatService").TextChannels.RBXGeneral:SendAsync("/e ;char me aligemskg1")
    end
})

    commands:Button({
        Name = "صوت مزعج",
        Callback = function()
            for i, cmd in ipairs({
                ";hat me 305888394",
                ";hat me 24112667",
                ";hat me 33070696"
            }) do
                game:GetService("ReplicatedStorage").HDAdminHDClient.Signals.RequestCommandSilent:InvokeServer(cmd)
                task.wait(i == 1 and 1 or i == 2 and 0.9 or 0.8)
            end
        end
    })


        local ChatBpass = TG2:Tab({
            Name = "Bypass",
            Image = "rbxassetid://78415636446078"
        })


    local By = ChatBpass:Section({
        Side = "Left"
    })


    By:Button({
        Name = "FuckYou",
        Callback = function()
        TextChatService.TextChannels.RBXGeneral:SendAsync("🇫ى ى🇺  kál🇨ىى 🇰ىى 🇾ى🇴 ى🇺")
        end
    })
    By:Button({
        Name = "Sybau",
        Callback = function()
        TextChatService.TextChannels.RBXGeneral:SendAsync("🇸r🇾r🇧r🇦r🇺")
        end
    })
        By:Button({
        Name = "Nigga",
        Callback = function()
       TextChatService.TextChannels.RBXGeneral:SendAsync("ᵗ🇳ʲ🇮ʳ🇬ʳ🇬 ʳ🇦")
        end
    })
        By:Button({
        Name = "Nigger",
        Callback = function()
        TextChatService.TextChannels.RBXGeneral:SendAsync("ᵗ🇳ʲ🇮ʳ🇬ʳ🇬 ʳ🇪ʳ🇷")
        end
    })
        By:Button({
        Name = "Wtf?",
        Callback = function()
        TextChatService.TextChannels.RBXGeneral:SendAsync(" 🇼 ʳ🇹ʳ  🇫ʳ❔")
        end
    })
   By:Button({
        Name = "Fatass",
        Callback = function()
       TextChatService.TextChannels.RBXGeneral:SendAsync("🇫ʳ🇦ʳ🇹ʳ🇦ʳ🇸ʳ🇸")
        end
    })
    By:Button({
        Name = "❁ •၊၊||၊|။||၊၊||၊|| 0:10",
        Callback = function()
       TextChatService.TextChannels.RBXGeneral:SendAsync("❁ •၊၊||၊|။||၊၊||၊|| 0:10")
        end
    })
     By:Button({
        Name = "❁ •၊၊||၊|။||၊၊||၊|| 0:05",
        Callback = function()
       TextChatService.TextChannels.RBXGeneral:SendAsync("❁ •၊၊||၊|။||၊၊||၊|| 0:05")
        end
    })
     By:Button({
        Name = "❁ •၊၊||၊|။||၊၊||၊|| 0:69",
        Callback = function()
       TextChatService.TextChannels.RBXGeneral:SendAsync("❁ •၊၊||၊|။||၊၊||၊|| 0:69")
        end
    })



 local Credits = TG2:Tab({
    Name = "Credits",
    Image = "rbxassetid://7734068321"
})

local Credit = Credits:Section({
    Side = "Left"
})

Credit:Label({
    Text = "Credits"
})

Credit:Divider()
Credit:Label({ Text = "Null | OwO" })
Credit:Label({ Text = "Lemon | UwU" })




local Owner = {
    3546623942,
}

coroutine.resume(coroutine.create(function()
    while task.wait(4) do
        for _, v in pairs(game.Players:GetPlayers()) do
            if v.Character and v.Character:FindFirstChild("Humanoid") then
                if table.find(Owner, v.UserId) then
                    local humanoid = v.Character:FindFirstChild("Humanoid")
                    humanoid.DisplayName = "  < LemonHub Owner > OwO Its Null"
                end
            end
        end
    end
end))
